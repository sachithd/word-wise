A Simple Dictionary App

Find the definition of a given word. The app uses the api provided by https://dictionaryapi.dev/ to
find the definition of a words. If there is a valid definition, it stores the definition in the local database so
if the user search for the same word again it will query the database before calling the API

1. Organise the project structure
2. Create the UI
3. Material Theming
4. Database Setup
5. API Integration